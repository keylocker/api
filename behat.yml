---
default:
  autoload:
    - %paths.base%/test/features/bootstrap
  suites:
    api_features:
      formatter:
      name: pretty
      contexts: [ ApiContext ]
      paths:
        features: %paths.base%/test/features
